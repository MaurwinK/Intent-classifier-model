apiVersion: serving.kserve.io/v1beta1
kind: InferenceService
metadata:
  name: intent-model
spec:
  predictor:
    model:
      modelFormat:
        name: sklearn
      storageUri: s3://mlops-demo-maurwin-dvc-bucket/intent-model
      resources:
        requests:
          cpu: "100m"
          memory: "512Mi"
        limits:
          cpu: "1"
          memory: "1Gi"